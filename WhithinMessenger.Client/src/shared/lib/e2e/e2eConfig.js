/**
 * Глобальный выключатель E2E на клиенте.
 *
 * Зеркалит бэкендовый флаг `E2eController.E2eTempDisabled`: пока там `true`, все
 * `/api/e2e/*` отвечают 503, поэтому клиент не должен ни загружать ключи устройства,
 * ни синхронизировать chat-key wraps, ни шифровать исходящие сообщения.
 *
 * Включать обратно нужно одновременно с бэкендом.
 */
export const E2E_ENABLED = false;

/** Текст вместо содержимого старых зашифрованных сообщений, пока E2E выключен. */
export const E2E_DISABLED_PLACEHOLDER_TEXT = 'Зашифрованное сообщение';
